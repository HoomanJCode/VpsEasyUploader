{
  "name": "vpseasyuploader-js-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend (vanilla JS) tests for VpsEasyUploader.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "happy-dom": "^20.10.6",
    "vitest": "^2.1.4"
  }
}
